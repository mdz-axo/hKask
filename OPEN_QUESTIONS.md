# Open Questions — hKask Architecture Audit

Generated by TASK 0–TASK 6 epistemic/cybernetic/deep-module analysis, 2026-06-09.
Updated 2026-06-10 after allosteric deletion, consent instrumentation, and GovernedTool tests.

---

## 1. Unclassifiable Modules (TASK 0)

### 1.1 `hkask-cli/src/curation_config.rs`

**Status:** ✅ DELETED. The file was a 63-line byte-for-byte duplicate of `hkask-cns::set_points::load_curation_thresholds()`. Both copies had zero callers anywhere in the codebase. The `load_curation_thresholds` function (duplicated in both files) was also deleted — `CurationThresholdConfig::default()` is used directly by callers instead.

### 1.2 `hkask-api/src/error.rs`

**Classification:** ACCIDENT (HTTP status code mapping — protocol artifact)

**Status:** RESOLVED — fine as-is. Per architecture master surface-vs-service boundary table.

---

## 2. Open-Loop Constraints (TASK 1)

### 2.1 OCAP Enforcement

**Status:** RESOLVED — OCAP gates are Prohibitions by design (Magna Carta P4). Not cybernetic loops. No code change needed.

### 2.2 Consent Enforcement

**Status:** ✅ DONE. `ConsentManager` now has an optional `NuEventSink`. When wired via `with_event_sink()`, every `has_consent()` denial emits a `cns.consent.denied` ν-event. This is OBSERVE-only — the denial remains terminal (Prohibition, not Guardrail).

### 2.3 SpecDriftAlert

**Status:** ✅ DONE. Added `CurationInput::SpecDriftResolved { spec_id, resolved_at }` to `hkask-types::loops::channels`. When a human resolves spec drift, the event flows through the existing `CurationInput` channel (same path as `SpecDrift` alerts). The `CurationLoop::sense()` handler logs it with `tracing::info!` — no automated revision, just observability closure.

---

## 3. Deletion Test Deferrals (TASK 2)

### 3.1 Type Migrations

**Status:** ❌ REJECTED. Moving `cns::CnsHealth`, `sovereignty::DataCategory`, `loops::*`, or `visibility::*` from `hkask-types` to destination crates would force `hkask-templates`, `hkask-cli`, and `hkask-services` to add heavy dependencies just for type definitions. These are domain vocabulary — they correctly live in `hkask-types`. The TASK 2 MERGE verdicts were wrong. `RBarThreshold` was the only genuinely dead type and it has been deleted.

### 3.2 SovereigntyService Split

**Status:** ❌ DOCUMENTATION ERROR. The architecture master's claim of "SovereigntyService with 9 public functions + 2 types" does not correspond to any actual module. Sovereignty enforcement is distributed across `hkask-types::sovereignty`, `hkask-agents::sovereignty`, and `hkask-services::verification`. No module exceeds 7 public items. The architecture master needs a documentation fix, not a code change.

---

## 4. Performance Boundaries (TASK 1)

### 4.1 Allosteric Gate Latency

**Status:** MOOT — the allosteric gate has been deleted. Binary thresholds have zero latency.

### 4.2 Context Window Channel Capacity

**Status:** OPEN — requires user decision.

**Question:** What are the acceptable bounds for condenser throughput (bytes/second) and compression ratio?

**Proposed resolution:** Define CNS SLA: throughput ≥ 1KB/s, compression ≥ 2:1, emit `cns.condenser.degraded` ν-event on degradation.

---

## 5. Registry/Template Impact (TASK 6)

### 5.1 Template Path Updates

**Status:** MOOT — type migrations were rejected. No template path changes needed.

### 5.2 hLexicon Mapping to MDS Categories

**Status:** OPEN — requires user decision.

**Question:** Should `LexiconTerm` carry an explicit `mds_category` field?

**Proposed resolution:** Add `mds_category: Option<MdsCategory>` to `LexiconTerm`. Backfill existing terms. Enables formal verification via `spec/graph/coherence`.

---

## 6. Test Coverage Gaps (TASK 4)

### 6.1 GovernedTool Membrane

**Status:** ✅ PARTIALLY DONE. 4 OCAP verification tests added (`legacy_exact_match`, `denies_wrong_tool`, `domain_match`, `domain_deny`). Full 6-step membrane integration test still needs mock infrastructure.

### 6.2 Service Layer Tests

**Status:** OPEN — `hkask-services` has 15 unit tests (chat + goal). Integration tests for cross-crate service functions (InferenceService, ComposeService, ArchivalService) need DB fixtures and/or running models.

---

## 7. Condensation Fixed Point (TASK 5)

**Status:** RESOLVED. Fixed point at **247** source files after allosteric deletion and duplicate removal.

| Iteration | Files | Delta | What changed |
|-----------|-------|-------|-------------|
| 0 | 254 | — | Baseline |
| 1 | 251 | -3 | variety.rs merge, SnapshotWriter/RepoManager deletion |
| 2 | 248 | -3 | Allosteric deletion (6 files) |
| 3 | 247 | -1 | curation_config.rs duplicate + dead load_curation_thresholds |

**Remaining irreducible complexity:**
- ~20 type-definition files in `hkask-types` (domain vocabulary)
- ~15 port trait files (hexagonal contracts)
- 10 MCP server `src/main.rs` files (P1 Prohibition prevents deduplication)
- ~10 surface files (CLI/API — presentation is by-definition shallow)

---

*Generated by the hKask epistemic/cybernetic architecture audit (TASK 0–TASK 6).*
*Version: v0.27.0 → v0.27.2 condensation pass.*