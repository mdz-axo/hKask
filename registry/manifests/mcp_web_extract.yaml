# MCP Web — Web Search & Extract Manifest
# ℏKask v0.21.0 — Planck's Constant of Agent Systems

manifest:
  id: mcp/web_extract
  name: MCP Web Extractor
  description: >
    Search and extract information from web sources.
    Supports source filtering, freshness constraints, and relevance ranking.
  version: 0.21.0
  editor: curator-or-human-admin
  visibility: Shared

# Energy Budget Configuration
energy:
  cap: 4096
  cost_per_token: 0.25
  alert_threshold: 0.8
  hard_limit: true

# Process Steps
steps:
  - ordinal: 1
    action: populate
    description: >
      Render web extraction template with query and parameters.
    renderer: minijinja
    template_ref: mcp/web_extract
    model_tier: null
    mcp: null
    energy_cap: 1000
    timeout_seconds: 10
    output_schema:
      rendered_prompt: string

  - ordinal: 2
    action: execute
    description: >
      Execute web search and extraction via MCP.
      Call Brave/Firecrawl search APIs.
    renderer: null
    template_ref: null
    model_tier: fast_local
    mcp: hkask-mcp-web
    energy_cap: 4096
    timeout_seconds: 30
    output_schema:
      results: array
      total_found: integer
      search_metadata: object

  - ordinal: 3
    action: feedback
    description: >
      Emit search outcome to CNS.
      Track result quality and source diversity.
    renderer: null
    template_ref: null
    model_tier: null
    mcp: hkask-cns
    energy_cap: 500
    timeout_seconds: 5
    feedback:
      variety_delta: integer
      result_count: integer
    output_schema:
      cns_event_id: string
      outcome_recorded: boolean

# Error Handling
error_handling:
  on_energy_exceeded: abort
  on_timeout: retry
  max_retries: 2
  retry_backoff_seconds: 1
  on_capability_denied: escalate
  on_search_failure: fallback

# OCAP Configuration
ocap:
  required_capabilities:
    - resource: template
      action: render
      template_id: mcp/web_extract
      energy_budget: 2000
    - resource: web
      action: search
      template_id: mcp/web_extract
      energy_budget: 4000
    - resource: cns
      action: emit
      template_id: mcp/web_extract
      energy_budget: 500
  delegation_chain_required: true
  signature_algorithm: ed25519
  capability_expiry_seconds: 3600
  template_scoped: true

# CNS Monitoring
cns:
  emit_spans: true
  span_namespace: cns.web.search
  variety_monitoring: true
  algedonic_threshold: 100
  escalation_target: Curator

# Audit Trail
audit:
  enabled: true
  log_level: info
  include_input: true
  include_output: true
  include_energy_cost: true
  include_cns_events: true
  include_search_metrics: true
