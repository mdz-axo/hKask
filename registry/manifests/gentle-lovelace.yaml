# Gentle Lovelace Process Manifest
# ℏKask v0.30.0 — Writing-quality evaluation as convergent PDCA

manifest:
  id: gentle-lovelace
  name: Gentle Lovelace
  description: >
    Convergent writing-quality evaluation process. Produces dimension diagnostics,
    computes convergence of quality assessment, and iterates until confidence in
    weakest-dimension recommendations is stable.
  functional_role: flowdef
  version: 0.30.0
  editor: curator-or-human-admin
  visibility: Public

convergence:
  threshold: 0.15
  improvement_ratio: 0.10
  improvement_gate: threshold_only
  max_iterations: 3
  min_iterations: 1
  convergence_field: step_2_result.convergence_metric
  on_not_reached: escalate

gas:
  cap: 100000
  cost_per_iteration: 100
  alert_threshold: 0.8
  hard_limit: true

rjoule:
  cap: 12000
  cost_per_token: 0.25
  alert_threshold: 0.8
  hard_limit: true

inputs:
  - name: document_content
    type: string
    required: true
  - name: dimension_scores
    type: object
    required: true
  - name: document_type
    type: string
    required: false
  - name: exemplar_context
    type: array
    required: false

steps:
  - ordinal: 1
    action: select
    description: Generate structured writing-quality diagnostics.
    renderer: minijinja
    template_ref: gentle-lovelace/replica-report
    gas_cap: 5000
    timeout_seconds: 60
    input_mapping:
      document_content: "{{ document_content }}"
      dimension_scores: "{{ dimension_scores }}"
      document_type: "{{ document_type | default('unspecified') }}"
      exemplar_context: "{{ exemplar_context | default([]) }}"

  - ordinal: 2
    action: select
    description: Compute normalized convergence metric for quality-evaluation cycle.
    renderer: minijinja
    template_ref: gentle-lovelace/gentle-convergence-check
    gas_cap: 2000
    timeout_seconds: 30
    input_mapping:
      primary_result: "{{ step_1_result }}"

  - ordinal: 3
    action: loop
    description: Re-enter quality-evaluation cycle if convergence is not met.
    input_mapping:
      loop_target: 1

error_handling:
  on_gas_exceeded: abort
  on_timeout: retry
  max_retries: 1
  retry_backoff_seconds: 1
  on_validation_failure: abort

ocap:
  required_capabilities:
    - resource: template
      action: render
      template_id: gentle-lovelace/replica-report
      gas_budget: 5000
    - resource: template
      action: render
      template_id: gentle-lovelace/gentle-convergence-check
      gas_budget: 2000
    - resource: manifest
      action: execute
      template_id: gentle-lovelace
      gas_budget: 1000
  delegation_chain_required: true
  signature_algorithm: ed25519
  capability_expiry_seconds: 3600
  template_scoped: true
