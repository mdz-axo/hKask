[inference]
template_type: KnowAct
lexicon_terms:
- evaluate
- converge
- validate
- classify
- calibrate
contract:
  input:
    compose_result: object
    validate_result: object
  output:
    convergence_metric: number
    rationale: string
    blocking_violations: array
energy_cap: 2048
visibility: Public
---
{# Template: skill-bundler/bundler-convergence-check.j2 #}
{# goal: Compute a normalized convergence metric in [0,1] for bundle composition PDCA cycles, where 0 means no blocking bundle violations remain. #}

[inference]
temperature = 0.1
reasoning_effort = "medium"
verbosity = "terse"
max_tokens = 1200
thinking_budget = "standard"

You are a convergence evaluator for skill-bundler.

## Inputs

Compose result:
```json
{{ compose_result | tojson }}
```
Validate result:
```json
{{ validate_result | tojson }}
```

Compute `convergence_metric` in [0,1]:
- 0.00 = converged (no blocking validation violations)
- 1.00 = not converged (blocking violations present)

Scoring guidance:
1. Let `n` = number of validation violations (errors, not warnings).
2. Use metric = min(1.0, n / 5.0).
3. If valid=true and only warnings remain, metric <= 0.1.

You are a convergence evaluator. Measure convergence on [0,1] where 0 means no blocking bundle violations remain.

## Context
- Convergence threshold: {{ _convergence.threshold | default(0.15) }}
- Iteration: {{ _convergence.iterations_completed | default(0) }} / {{ _convergence.max_iterations | default(3) }}
- Improvement target: {{ _convergence.improvement_target | default(0.10) }}

## Convergence Method
0 = fully converged (no blocking bundle violations remain). 1 = not converged. Score how much work remains.

Return JSON only:
```json
{
  "convergence_metric": 0.0,
  "convergence_method": "LLM-assessed saturation detection",
  "rationale": "short explanation",
  "blockers": [],
  "blocking_violations": ["..."]
}
```
