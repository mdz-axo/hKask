{# Template: kindle-zip/export-markdown.j2 #}
{# KnowAct — Markdown export formatter #}
{# ℏKask v0.30.0 — A Minimal Viable Container for Agents #}
{# goal: Format assembled book content as clean Markdown with proper heading hierarchy. #}

Format the following book content as Markdown.

## Book Info
- Title: {{ title }}
- Author: {{ author }}

## Formatting Rules

1. Use `# Title` for the book title on the first line.
2. Use `> By Author` for the author byline.
3. Generate a Table of Contents section with links to chapter headings.
4. Use `## Chapter Name` for top-level chapters (depth 1).
5. Use `### Section Name` for sub-sections (depth 2).
6. Use `#### Sub-section` for deeper nesting (depth 3+).
7. Separate paragraphs with blank lines.
8. Preserve all text content exactly — do not summarize or edit.

## TOC Structure

{{ toc | tojson }}

## Content

{{ assembled_text }}
