# Infrastructure config manifest for memory
# hKask v0.28.0
# This directory contains memory subsystem selectors and template configs.
# Not a skill template crate — no [inference] .j2 templates.

infrastructure:
  name: memory
  version: "0.28.0"
  description: >
    Memory subsystem configuration: storage selectors and template dispatch
    for conversation summarization, condenser integration, and context
    window management.
  role: config
  contains:
    - selectors/
    - templates/