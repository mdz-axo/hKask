# Infrastructure config manifest for memory
# hKask v0.28.0
# Memory operation templates — selector (operation-selector.j2) and
# operation templates (remember.j2, recall.j2) for the memory-ops FlowDef pipeline.

infrastructure:
  name: memory
  version: "0.28.0"
  description: >
    Memory operation templates — selector (operation-selector.j2) and
    operation templates (remember.j2, recall.j2) for the memory-ops
    FlowDef pipeline.
  role: config
  contains:
    - selectors/
    - templates/
