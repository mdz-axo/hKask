# Infrastructure config manifest for cns
# hKask v0.28.0
# This directory contains CNS monitoring templates — alert selector
# (alert-selector.j2) for the cns-monitoring FlowDef pipeline.

infrastructure:
  name: cns
  version: "0.28.0"
  description: >
    CNS (Cybernetic Nervous System) selector configuration. Maps CNS span
    names to routing and alerting behavior.
  role: config
  contains:
    - selectors/
