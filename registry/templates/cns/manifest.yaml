# Infrastructure config manifest for cns
# hKask v0.30.0
# CNS alerting is handled programmatically by the CyberneticsLoop.
# No template-based selectors remain.

infrastructure:
  name: cns
  version: "0.30.0"
  description: >
    CNS (Cybernetic Nervous System) infrastructure config.
    Alert classification and escalation are programmatic.
  role: config
