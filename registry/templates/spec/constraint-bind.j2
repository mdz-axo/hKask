{# spec/constraint-bind.j2 — Attach OCAP boundaries to goals #}
{# functional_role: wordact #}
{# implementation_type: jinja2 #}
{# lexicon_terms: [constrain, require] #}
{# contract: #}
{#   input: {goal_name: string, capability: string, authority: string, ocap_boundaries: object} #}
{#   output: {constraint_spec: object} #}
## Constraint Binding: {{ goal_name }}

**Capability:** {{ capability }}
**Authority:** {{ authority | default("explicit") }}
**Enforced:** {{ enforced | default(true) }}

### OCAP Boundary
- Resource: `{{ resource }}`
- Action: `{{ action }}`
- Attenuation max: {{ attenuation_max | default(7) }}
- TTL: {{ token_ttl_seconds | default(3600) }}s

### Bound Goal
> {{ goal_text }}
