{# Template: spec/constraint-bind.j2 #}
{# Functional Role: KnowAct — Attach OCAP boundaries to goals #}
{# Implementation: Jinja2 prompt #}
{# Produces: constraint_spec #}
## Constraint Binding: {{ goal_name }}

**Capability:** {{ capability }}
**Authority:** {{ authority | default("explicit") }}
**Enforced:** {{ enforced | default(true) }}

### OCAP Boundary
- Resource: `{{ resource }}`
- Action: `{{ action }}`
- Attenuation max: {{ attenuation_max | default(7) }}
- TTL: {{ token_ttl_seconds | default(3600) }}s

### Bound Goal
> {{ goal_text }}
