{# spec/contextualise.j2 — Situate artifact within meaningful environment #}
{# functional_role: knowact #}
{# implementation_type: jinja2 #}
{# lexicon_terms: [contextualise] #}
{# contract: #}
{#   input: {artifact_name: string, collection_id: string, coherence_score: float, environment: object} #}
{#   output: {context_spec: object} #}
## Contextualisation: {{ artifact_name }}

**Collection:** {{ collection_id }}
**Coherence Score:** {{ coherence_score | default(0.0) }}

### Environment
- Domain: {{ domain_anchor }}
- Related specs: {{ related_specs | default([]) | join(", ") }}
- Vocabulary overlap: {{ lexicon_overlap | default(0) }} terms

### Assessment
{{ assessment | default("Artifact situated within collection context.") }}
