{# spec/curate-collection.j2 — Evaluate collection coherence and completeness #}
{# functional_role: knowact #}
{# implementation_type: jinja2 #}
{# lexicon_terms: [curate, cultivate] #}
{# contract: #}
{#   input: {collection_id: string, decision: string, coherence_score: float, threshold: float, categories: array, categories_missing: array, rationale: string} #}
{#   output: {curation_spec: object} #}
## Curation Report

**Collection:** {{ collection_id | default("default") }}
**Decision:** {{ decision | default("Revise") }}
**Coherence:** {{ coherence_score | default(0.0) }} / {{ threshold | default(0.7) }}

### Categories
{% for cat in categories %}
- **{{ cat.name }}**: {{ "complete" if cat.complete else "incomplete" }} ({{ cat.spec_count }} specs)
{% endfor %}

### Missing Categories
{% for cat in categories_missing %}
- {{ cat }}
{% endfor %}

### Rationale
{{ rationale | default("Collection requires further specification to reach coherence threshold.") }}
