# hkask-adapter

Trained adapter lifecycle for hKask.

Manages LoRA adapters and other trained model artifacts.

## Features

- **Adapter store** — versioned, named adapter registry
- **Expertise** — adapter capability descriptions
- **Endpoint lifecycle** — adapter deployment and serving
- **Provider cost model** — cost estimation per provider
