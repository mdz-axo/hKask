[package]
name = "hkask-memory-fuzz"
version = "0.0.0"
publish = false
edition.workspace = true

[package.metadata]
cargo-fuzz = true

[[test]]
name = "memory_fuzz"
path = "fuzz_targets/memory_fuzz.rs"

[dependencies]
bolero = { workspace = true }
hkask-memory = { path = ".." }
