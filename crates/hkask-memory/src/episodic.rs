//! Episodic memory pipeline

pub struct EpisodicMemory;

impl EpisodicMemory {
    pub fn new() -> Self {
        Self
    }
}

impl Default for EpisodicMemory {
    fn default() -> Self {
        Self::new()
    }
}
