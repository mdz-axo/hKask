//! Agent deliberation

pub struct Deliberation;

impl Deliberation {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Deliberation {
    fn default() -> Self {
        Self::new()
    }
}
