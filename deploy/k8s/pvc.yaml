apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hkask-data
  namespace: hkask
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # storageClassName: your-storage-class  # uncomment for non-default
