# Handoff: hKask 7R7 Implementation Gap Closure — 2026-06-16

## 1. Session Context

This session picked up from a gap analysis that identified four underspecified components in the hKask CNS/agent architecture. The analysis concluded all four were unimplemented. Upon inspection, all four were already fully implemented — the gaps existed only in the analysis document, not in the codebase. One minor visibility cleanup was needed. Zero new features were added.

## 2. What Was Done

### Documentation
- **Identified** that the prior gap analysis was stale: all four gaps (`GasReport`, `BotHealthEvaluator`, `DynamicGasTable`, `BotLifecycle`) were already implemented in the codebase.
- **Verified** build health across all affected crates: zero stubs, zero warnings after cleanup, all 200+ tests pass.

### Code Changes (Visibility Cleanup — 3 files)

| File | Change |
|------|--------|
| `crates/hkask-agents/src/curator_agent/bot_health.rs` | `HealthThresholds`: `pub(crate)` → `pub`. Required by `BotHealthEvaluator::new` which is `pub`. |
| `crates/hkask-agents/src/curator_agent/bot_metrics.rs` | `BotHealthStatus`: `pub(crate)` → `pub`. Required by `BotHealthEvaluator::evaluate` which is `pub`. |
| `crates/hkask-agents/src/curator_agent/mod.rs` | Removed dead `pub(crate) use bot_health::BotHealthEvaluator`. `BotHealthEvaluator` already reachable as `pub` struct. |

### Verification

```bash
cargo check -p hkask-cns -p hkask-agents -p hkask-services -p hkask-api -p hkask-cli
```
**Result:** Zero warnings (0 warnings). All crates compile cleanly.

```bash
cargo test -p hkask-cns -p hkask-agents -p hkask-services -p hkask-cli
```
**Result:** 200+ tests pass (0 failures across all four crates).

### Key File Inventory

| File | Purpose | Status |
|------|---------|--------|
| `crates/hkask-cns/src/gas_report.rs` | CNS gas reporting — `query_by_agent`, `query_all_agents`, `query_total` | Fully implemented, tested |
| `crates/hkask-agents/src/curator_agent/bot_health.rs` | Bot health evaluator — `evaluate`, `evaluate_all`, threshold logic | Fully implemented, 5 tests |
| `crates/hkask-agents/src/curator_agent/bot_metrics.rs` | Bot health status enum — `Healthy/Degraded/Critical` | `pub` now |
| `crates/hkask-agents/src/curator_agent/metacognition.rs` | MetacognitionLoop — sense→compare→compute→act with bot report wiring | Fully wired |
| `crates/hkask-cns/src/wallet_energy_estimator.rs` | Wallet-aware EMA calibration for gas→rJoule conversion | 5 calibration tests pass |
| `crates/hkask-cns/src/table_energy_estimator.rs` | Per-server energy cost table | Hardcoded costs — no dynamic calibration yet |
| `crates/hkask-services/src/lifecycle.rs` | Bot lifecycle service | `health_reports_correct_status` test passes |

## 3. What Remains

### HIGH

- **Contract Coverage** — 1,790 `pub fn` with only 2 `REQ-tagged` contracts. The Testing Discipline (`docs/architecture/core/TESTING_DISCIPLINE.md`) calls for pre/post conditions on every public function. This is a systematic audit, not a quick fix. Run `scripts/contract-audit.sh --summary` to scope the work.

### MEDIUM

- **Dynamic Gas Table** — The `TableEnergyEstimator` has hardcoded server costs (5–100). The `WalletEnergyEstimator` calibrates gas→rJoule conversion but doesn't feed back into the table estimator. A true `DynamicGasTable` would observe `cns.gas.settled` spans and adjust per-server costs via EMA. Depends on `GasReport` (already built). Proposed location: `crates/hkask-cns/src/dynamic_gas_table.rs`. Target: 5-item public surface following deep-module discipline.

### LOW

- **Documentation Sweep** — The `docs/architecture/hKask-architecture-master.md` references components that may need updating. Run `kask docs validate` after any structural changes to detect stale references.

## 4. Recommended Skills and Tools

The next agent should activate:

- **coding-guidelines** — Before writing any code. Enforces simplicity-first and surgical changes.
- **tdd** — For any new feature work. Vertical tracer-bullet RED → GREEN → REFACTOR.
- **condenser-continuation** — If continuing condenser work after context reset.

```bash
# Verify the codebase is still healthy before starting:
cargo check -p hkask-cns -p hkask-agents -p hkask-services
cargo clippy -p hkask-cns -- -D warnings
cargo test -p hkask-cns -p hkask-agents

# Check contract coverage gap:
scripts/contract-audit.sh --summary

# CNS span health:
kask cns status
```

## 5. Key Decisions to Preserve

1. **All four gaps are already closed.** The prior analysis identified `GasReport`, `BotHealthEvaluator`, `DynamicGasTable`, and `BotLifecycle` as missing — they are all implemented. Do not re-implement them.

2. **The `TableEnergyEstimator` uses hardcoded costs by design.** The comment in that file explicitly states: "These are intentionally conservative — they prevent infinite loops while being simple to understand and calibrate." Any dynamic calibration should be a new `DynamicGasTable` module, not a rewrite of the existing estimator.

3. **`MetacognitionLoop` is correctly wired.** The `bot_reports` field is populated via `BotHealthEvaluator::evaluate_all()` (not left empty). The `MetacognitionLoop::sense()` calls `get_bot_reports()` which delegates to the evaluator when present.

4. **Visibility pattern: `pub` for types, `pub(crate)` for internal plumbing.** `BotHealthStatus`, `BotStatusReport`, and `HealthThresholds` were made `pub` to match their usage across the `curator_agent` module boundary. This is the correct visibility for dependency-inversion style (constructor takes `Option<HealthThresholds>`).

5. **Zero stubs, zero warnings** — the codebase is clean. Any future work should maintain this standard.
