# Documentation Archive

This directory is for local maintainer reference only.
Archived documents are recoverable via Git history:

```bash
# Find deleted documents
git log --diff-filter=D -- docs/

# Restore a deleted document
git show <sha>:docs/architecture/<filename>.md
```

**Git ignores this directory** — files here are not tracked.

