---
title: "Hetzner K3s Deployment — Blocking Issues"
audience: [developers]
last_updated: 2026-06-20
version: "0.30.0"
status: "Blocked — Infrastructure Access Required"
domain: "Deployment"
mds_categories: [lifecycle]
---

# Hetzner K3s Deployment — Blocking Issues

The Hetzner environment is provisioned (API key, storage bucket, cloud server) but the following items cannot be completed without direct access to the Hetzner K3s cluster from the development environment.

## Blockers

| # | Task | Blocker | Resolution Path |
|---|------|---------|-----------------|
| 1 | K3s cluster bootstrap automation | Cannot run `hetzner-k3s create` or `kubectl` from development machine. Requires Hetzner API calls that provision VPS instances. | After gaining SSH/kubectl access to the Hetzner environment, write a bootstrap script that automates `hetzner-k3s create` or Cloudfleet cluster provisioning. |
| 2 | cert-manager + Let's Encrypt setup | Depends on #1 (no K3s cluster to install cert-manager on). | Once K3s is running, apply cert-manager manifests and create a ClusterIssuer. Scriptable as a single `kubectl apply`. |
| 3 | Hetzner Object Storage bucket provisioning | API call can be implemented locally (HTTP PUT to S3-compatible endpoint). Cannot validate without running Litestream against the bucket. | Implement `HetznerClient::create_object_storage_bucket()` using the S3-compatible API. Test when access is available. |
| 4 | `kask pod export k8s` validation | Manifests are generated correctly (verified by code review), but cannot be applied and tested end-to-end without a running K3s cluster. | Apply manifests against provisioned cluster, verify StatefulSet creates pods, Litestream-sidecar connects to Hetzner OS, Conduit starts, kask serves health endpoint. |
| 5 | Integration test: full lifecycle | Depends on #1-4. | End-to-end: create pod → deploy to K3s → Litestream backs up to Hetzner OS → activate/deactivate via kubectl → destroy. |
| 6 | CNS span for cloud provider health | Code exists. Cannot emit real spans without running infrastructure. | Verify span emission once K3s is running. |
| 7 | Automated failover (Fly.io → Hetzner) | Depends on both Fly.io and Hetzner being operational simultaneously. | Cross-provider migration test: generate K8s manifests → apply → Litestream restores from same object storage bucket → verify pod state identical. |

## What Exists and Is Testable

| Artifact | Status | Location |
|----------|--------|----------|
| HetznerClient (5 methods) | ✅ Compiles | `crates/hkask-cli/src/cloud/hetzner.rs` |
| `kask pod export k8s` (6 YAML manifests) | ✅ Compiles | `crates/hkask-cli/src/commands/pod.rs::export_k8s` |
| kubectl lifecycle (cloud_activate_k8s, cloud_deactivate_k8s) | ✅ Compiles | `crates/hkask-cli/src/commands/pod.rs` |
| K8s StatefulSet with init containers | ✅ Correct | Generated by export_k8s |
| K8s NetworkPolicy (per-namespace isolation) | ✅ Correct | Generated by export_k8s |
| Litestream config baked into ConfigMap | ✅ Correct | Generated by export_k8s |
| Conduit config baked into ConfigMap | ✅ Correct | Generated by export_k8s |

## Recommendation

Table Hetzner work until direct cluster access is available. Focus on the Fly.io deployment path which has no infrastructure blockers. The Hetzner code is complete and ready to test — it just needs a cluster to run against.
