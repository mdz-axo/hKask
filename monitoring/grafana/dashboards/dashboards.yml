apiVersion: 1

providers:
  - name: 'hKask Dashboards'
    orgId: 1
    folder: 'hKask'
    folderUid: 'hkask'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
